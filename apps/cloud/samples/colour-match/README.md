# Colour Match

Install and set up the required OpenAI API key, then run “Check Colour Match connection” from the command palette. This sample reports the API status; it does not change your project.
