{
  "name": "powermove",
  "version": "1.0.0",
  "private": true,
  "description": "A GPU-native, AI-authored motion editor for macOS.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:mac": "./scripts/build-macos-app.sh",
    "build:mac:release": "./scripts/build-macos-app.sh --release"
  },
  "engines": {
    "node": ">=20"
  }
}
